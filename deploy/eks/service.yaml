apiVersion: v1
kind: Service
metadata:
  name: pageindex-rag
  labels:
    app: pageindex-rag
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: pageindex-rag
